{
  "projectSettings": {
    "framework": "nextjs"
  },
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "publicSource": "/public",
  "ignoreCommand": "git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT -- . ':!package-lock.json'"
}
